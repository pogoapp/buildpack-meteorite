#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
addons:
  - mongohq:free
config_vars:
  PATH: .meteor/heroku_build/bin:node_modules/.bin:/usr/local/bin:/usr/bin:/bin
default_process_types:
  web: MONGO_URL=\$MONGOHQ_URL node .meteor/heroku_build/app/main.js
EOF
